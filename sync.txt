🎯 AKLASBELA-TV SYNC INSTRUCTIONS

Your last run failed because of hidden Windows characters (\r). 
To fix this permanently, run these THREE commands in order:

1. Download/Create the clean script:
   nano sync.sh
   (Paste the code from the updated sync.sh file)

2. STRIP WINDOWS CHARACTERS (Crucial step):
   sed -i 's/\r$//' sync.sh

3. Set permissions and run:
   chmod +x sync.sh
   ./sync.sh

---------------------------------------------------------
CLEAN SCRIPT CONTENT BELOW:
---------------------------------------------------------
#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$DIR"
fuser -k 3000/tcp 3001/tcp 3002/tcp 2>/dev/null || true
git fetch --all
git reset --hard origin/main
npm install
npm run build
cd backend
npm install
pm2 delete aklasbela-backend 2>/dev/null || true
pm2 start server.js --name "aklasbela-backend"
cd ..
echo "SYNC COMPLETE"
